To create the database you must have an account that has
username = "testUser";
password = "badpassword%%";
and then run
http://localhost/reversi/SQLsetup.php

This will set up the SQL database.

The main page is
http://localhost/reversi/homepage.html
